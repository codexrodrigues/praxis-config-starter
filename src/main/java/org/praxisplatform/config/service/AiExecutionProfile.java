package org.praxisplatform.config.service;

/**
 * Internal execution purpose used to select provider capabilities without exposing
 * provider-specific configuration to HTTP consumers.
 */
public enum AiExecutionProfile {
    AGENTIC_AUTHORING
}
